# Núcleo (backup, AST, sandbox, heurística, memória, contexto): só biblioteca padrão.
# SDK oficial (ClaudeFixStrategy / AnthropicProvider). Testado com 1.3.0.
anthropic>=1.3
# Visão (OpenCV + numpy). Use opencv-python se precisar de janelas/GUI.
opencv-python-headless>=4.10
numpy>=1.26
# Captura de tela (ScreenSource). Opcional.
mss>=9.0
# OCR opcional: pip install pytesseract e instale o binário `tesseract` no sistema.
